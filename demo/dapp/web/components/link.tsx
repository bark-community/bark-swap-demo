// Example usage in another component
import CustomLink from './link';

const SomeComponent = () => (
  <div>
    <CustomLink href="/about" className="custom-style">
      Go to About
    </CustomLink>
  </div>
);
